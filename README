## MZES Distribution

### Build

Copy the `mzes_distro.make` file to your system and run with `drush make`

    $ curl -O https://github.com/mzes/mzes/raw/dev/mzes_distro.make
    $ drush make --working-copy mzes_distro.make [directory]